# Release workflow - builds for Linux aarch64 (ARM64)
# Uses Ubuntu 22.04 (glibc 2.35) cross-compilation for compatibility with Debian 12 (glibc 2.36)

name: Release

on:
  push:
    tags:
      - 'v*'

env:
  CARGO_TERM_COLOR: always

jobs:
  build-linux-aarch64:
    name: Build Linux (aarch64)
    runs-on: ubuntu-22.04
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust with aarch64 target
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-unknown-linux-gnu

      - name: Add arm64 architecture and sources
        run: |
          sudo dpkg --add-architecture arm64
          # Add explicit arm64 sources from ports.ubuntu.com (doesn't have security pocket issues)
          echo 'deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports/ jammy main restricted universe multiverse' | sudo tee /etc/apt/sources.list.d/arm64.list
          echo 'deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports/ jammy-updates main restricted universe multiverse' | sudo tee -a /etc/apt/sources.list.d/arm64.list
          sudo apt-get update

      - name: Install dependencies
        run: |
          sudo apt-get install -y crossbuild-essential-arm64 pkg-config
          sudo apt-get install -y libgtk-3-dev:arm64 libxcb-shape0-dev:arm64 libxcb-xfixes0-dev:arm64 libfontconfig1-dev:arm64

      - name: Install cargo-deb
        run: cargo install cargo-deb

      - name: Build release
        env:
          PKG_CONFIG_ALLOW_CROSS: 1
          PKG_CONFIG_PATH: /usr/lib/aarch64-linux-gnu/pkgconfig
          PKG_CONFIG_SYSROOT_DIR: /
        run: cargo build --release --target aarch64-unknown-linux-gnu

      - name: Create release archive
        run: |
          mkdir release
          cp target/aarch64-unknown-linux-gnu/release/ferrite release/
          tar -czvf ferrite-linux-aarch64.tar.gz -C release .

      - name: Build .deb package (Debian/Ubuntu)
        run: cargo deb --no-build --target aarch64-unknown-linux-gnu

      - name: Copy packages to workspace root
        run: |
          cp target/debian/*.deb ./ferrite-editor_arm64.deb

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          name: Ferrite ${{ github.ref_name }}
          draft: false
          prerelease: ${{ contains(github.ref_name, '-') }}
          generate_release_notes: true
          files: |
            ferrite-linux-aarch64.tar.gz
            ferrite-editor_arm64.deb
